# Flink Kubernetes Operator — manages FlinkDeployment CRs in vega namespace
watchNamespaces:
  - vega
jobServiceAccount:
  create: true
webhook:
  create: true
resources:
  requests:
    memory: 512Mi
    cpu: 250m
  limits:
    memory: 1Gi
    cpu: 500m
