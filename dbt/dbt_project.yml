name: vega
version: 1.0.0
config-version: 2
profile: vega

# Vega analytics: staging views and mart tables on Databricks/Iceberg

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

models:
  vega:
    staging:
      +materialized: view
    marts:
      +materialized: table
